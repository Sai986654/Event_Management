# Copy to .env for local dev (not committed). EAS: set EXPO_PUBLIC_* under Project → Environment variables.
#
# Production API (Render):
EXPO_PUBLIC_API_URL=https://event-management-9i4d.onrender.com/api
#
# Local backend (Android emulator → host):
# EXPO_PUBLIC_API_URL=http://10.0.2.2:5000/api
# Physical device on same Wi‑Fi (use your PC LAN IP):
# EXPO_PUBLIC_API_URL=http://192.168.1.100:5000/api
